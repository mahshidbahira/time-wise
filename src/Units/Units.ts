const MILLISECOND: number = 1;
const SECOND: number = 1000 * MILLISECOND;
const MINUTE: number = 60 * SECOND;
const HOUR: number = 60 * MINUTE;
const DAY: number = 24 * HOUR;

export { MILLISECOND, SECOND, MINUTE, HOUR, DAY };
